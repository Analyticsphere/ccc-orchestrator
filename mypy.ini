[mypy]
# Use explicit package bases to avoid module name conflicts
explicit_package_bases = True
mypy_path = dags

# Ignore missing imports for third-party libraries that don't have type stubs
[mypy-airflow.*]
ignore_missing_imports = True

[mypy-google.*]
ignore_missing_imports = True
